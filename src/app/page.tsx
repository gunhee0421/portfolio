'use client'

import IntroActivity from '@/components/IntroActivity'
import Image from 'next/image'

const Home = () => {
  return (
    <div className="flex items-center justify-center min-h-screen bg-black opacity-85">
      <IntroActivity />
    </div>
  )
}
export default Home
